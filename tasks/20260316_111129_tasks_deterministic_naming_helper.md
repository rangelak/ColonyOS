## Relevant Files

- `src/colonyos_pm/naming.py` - Deterministic helper for PRD filenames, task filenames, and changelog headings, with a small CLI for agent use.
- `src/colonyos_pm/__init__.py` - Re-exports the naming helpers for package consumers.
- `tests/test_naming.py` - Test coverage for slug normalization, timestamp validation, paired-name generation, and CLI behavior.
- `.cursor/rules/create_prd.mdc` - Updated to require the naming helper for PRD filenames and changelog headings.
- `.cursor/rules/generate_tasks.mdc` - Updated to require the naming helper when deriving task filenames from PRDs.
- `.cursor/rules/track_work.mdc` - Updated to require helper-generated task filenames for new task logs.
- `START_HERE.md` - Updated to document the deterministic naming helper for contributors.
- `README.md` - Reviewed for impact during this task; no changes required because operational detail belongs in `START_HERE.md`.
- `tasks/20260316_111129_tasks_deterministic_naming_helper.md` - Task tracking for the naming helper addition.
- `tasks/CHANGELOG.md` - Repository changelog updated with the helper and rule integration.

### Notes

- The naming helper exists specifically so planning filenames are generated by code instead of being hand-formatted by the AI.
- `README.md` was reviewed and did not require changes because it intentionally defers setup and operational detail to `START_HERE.md`.

## Tasks

- [x] 1.0 Add a deterministic planning-name generator to the Python package
  - [x] 1.1 Create a helper module that generates timestamped PRD filenames, task filenames, and changelog headings from a feature label.
  - [x] 1.2 Add a helper that derives the paired task filename from an existing PRD path.
  - [x] 1.3 Expose a small CLI so agents can call the helper instead of manually formatting names.
- [x] 2.0 Add tests for the naming helper
  - [x] 2.1 Cover slug normalization and timestamp generation.
  - [x] 2.2 Cover paired PRD/task filename generation and PRD-path parsing.
  - [x] 2.3 Cover CLI output for bundle and task-from-prd commands.
- [x] 3.0 Wire the helper into repo conventions and onboarding
  - [x] 3.1 Update the PRD rule to require the helper when generating PRD filenames and changelog headings.
  - [x] 3.2 Update the task-generation and work-tracking rules to require the helper for task filenames.
  - [x] 3.3 Update `START_HERE.md` to document the helper and record that `README.md` needed no change.
