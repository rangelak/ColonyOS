# Review by Linus Torvalds (Round 1)

The facts are clear. Here's my review:

---

VERDICT: request-changes

FINDINGS:
- [colonyos/add_feature]: Branch does not exist. There are 40+ `colonyos/*` branches, but none named `colonyos/add_feature`.
- [cOS_prds/20260330_002522_prd_add_feature.md]: PRD file does not exist. No file matching this name exists in the `cOS_prds/` directory.
- [cOS_tasks/]: No corresponding task file exists for "add_feature".
- [git diff]: Zero diff. There is nothing to review — no code was written, no files were changed, no commits were made.

SYNTHESIS:
Let me be blunt, because that's what you need to hear: **there is nothing here to review.** The branch `colonyos/add_feature` does not exist. The PRD `cOS_prds/20260330_002522_prd_add_feature.md` does not exist. There is no task file, no code diff, no implementation — nothing. Zero. Zilch. The empty set.

This is a pipeline control-flow bug, pure and simple. The original prompt "add feature" is a content-free string — it specifies nothing. The planning phase correctly refused to generate artifacts because you can't plan something that hasn't been defined. But then the orchestrator blundered forward into review and fix phases anyway, asking me to review a nonexistent branch against a nonexistent PRD. That's like asking me to review a patch that was never written against a spec that was never drafted.

I don't review vapor. I review code. Show me actual data structures, actual control flow, actual changes to actual files — and I'll tell you whether they're correct or merely clever. Until then, this pipeline run needs to be aborted and restarted from the PRD phase with a **concrete, specific feature description**.

Fix your orchestrator's invariant: never advance past the planning phase if the PRD and task artifacts don't exist on disk. That's a one-line guard check. Ship it.