# Review by Principal Systems Engineer (Google/Stripe caliber) (Round 1)

## Principal Systems Engineer — Review of `colonyos/add_feature`

### Artifact Verification

| Artifact | Status |
|----------|--------|
| Branch `colonyos/add_feature` | ❌ **Does not exist** (no local or remote branch) |
| PRD `cOS_prds/20260330_002602_prd_add_feature.md` | ❌ **Not found** |
| PRD `cOS_prds/20260330_002235_prd_add_feature.md` | ❌ **Not found** |
| Task file for `add_feature` | ❌ **Not found** |
| Code diff | ❌ **None** — currently on `colonyos/continue_the_last_plan`, no `add_feature` branch exists |

### Checklist

- [ ] **Completeness**: Cannot assess — no PRD, no tasks, no branch, no code.
- [ ] **Quality**: Cannot assess — nothing to evaluate.
- [ ] **Safety**: Cannot assess — nothing to evaluate.

---

VERDICT: request-changes

FINDINGS:
- [colonyos/add_feature]: Branch does not exist — `git branch -a` shows no match. Zero lines of code were implemented.
- [cOS_prds/20260330_002602_prd_add_feature.md]: PRD file does not exist on disk. The planning phase never produced an artifact because the input "add feature" carries zero information content — there is no feature to specify.
- [cOS_tasks/]: No corresponding task file exists. The pipeline had nothing to decompose.
- [pipeline/orchestrator]: **This is a control-flow invariant violation.** The orchestrator advanced through plan → implement → review phases without enforcing the precondition that upstream artifacts must exist before downstream phases execute. In a production system (Google SRE terms), this is equivalent to a missing readiness check — the pipeline should hard-fail at the implement gate if `PRD file not found` or `branch not created`, not silently proceed. The blast radius is wasted compute cycles and reviewer fatigue across now **five** iterations of confirming the same null state.

SYNTHESIS:
As a systems engineer, the most concerning aspect here is not the missing code — it's the **missing invariant enforcement in the pipeline itself**. A well-designed orchestrator treats phase transitions as guarded state machines: `plan → implement` requires `(PRD exists ∧ tasks exist)`, and `implement → review` requires `(branch exists ∧ diff non-empty)`. The current pipeline violates both guards, allowing degenerate inputs to cascade through every phase and burn budget confirming emptiness. This is the kind of silent failure that, at 3am, would page an on-call engineer repeatedly for a no-op. **No amount of review iterations can fix this** — the input "add feature" is an empty specification. To move forward: either (1) provide a concrete feature description and restart from the PRD phase, or (2) redirect the pipeline to the existing CLI decomposition feature on branch `colonyos/colonyos_cli_py`, which has a complete PRD and task file ready for implementation.